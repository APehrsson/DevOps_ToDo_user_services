apiVersion: v1
kind: Service
metadata:
  name: todo-user-services
spec:
  selector:
    app: todo-user-services
  ports:
    - protocol: TCP
      port: 4001
      targetPort: 4001
  type: ClusterIP

